/**
 *
 *   --open    在服务器启动时打开浏览器
 --mode    指定环境模式 (默认值：development)
 --host    指定 host (默认值：0.0.0.0)
 --port    指定 port (默认值：8080)
 --https   使用 https (默认值：false)
 * */
document.write('<h1>--open    在服务器启动时打开浏览器</h1>');
